% house-style.tex — tiny, CI-safe styling helpers only
% Lists
\setlist[itemize]{leftmargin=1.5em}
\setlist[enumerate]{leftmargin=1.5em}

% Tcolorbox note (used sparingly; safe defaults)
\tcbset{
  colback=white, colframe=black!15!gray,
  boxrule=0.3pt, arc=1pt, left=6pt, right=6pt, top=6pt, bottom=6pt
}

% Small helpers
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}
